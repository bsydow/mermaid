import { AbstractMermaidTokenBuilder } from '../common/index.js';

export class DomainStoryTokenBuilder extends AbstractMermaidTokenBuilder {
  public constructor() {
    super(['domainstorytelling-beta']);
  }
}
